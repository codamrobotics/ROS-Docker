name: Linux

on: [pull_request, workflow_dispatch]
#on: [push, pull_request, workflow_dispatch]

jobs:
  ubuntu:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository and submodules
      uses: actions/checkout@v2
      with:
        submodules: recursive

    - name: Check shell compatibility for workspacing
      run: |
          bash ./ros-docker.sh workspace --dryrun /home bash
          bash ./ros-docker.sh workspace --dryrun -c /home bash
          bash ./ros-docker.sh workspace --dryrun -r /home bash
          bash ./ros-docker.sh workspace --dryrun -c -r /home bash

    - name: Check shell compatibility for packaging
      run: |
          bash ./ros-docker.sh package --dryrun /home bash
          bash ./ros-docker.sh package --dryrun -c /home bash
          bash ./ros-docker.sh package --dryrun -r /home bash
          bash ./ros-docker.sh package --dryrun -c -r /home bash
